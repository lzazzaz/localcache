1、前期穿透控制， 提前放入请求，让内容得到更新。 过期则返回null。默认值是缓存时间的0.5 时进行放行

2、过期时穿透控制，过期时-->过期后-->0.5时间内进行穿透控制。 超过这

-值代表提前控制， 值的范围为0<=0 <1
+值代表延后几秒控制。值的范围为0<=0 <1

0值相当于在瞬间进行控制，实际情况这种瞬间是不存在的，效果相当于不做穿透控制


local cache 支持 mybatis 
local cache 支持 hibernate


